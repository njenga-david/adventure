import { TalkativeDirective } from './talkative.directive';

describe('TalkativeDirective', () => {
  it('should create an instance', () => {
    const directive = new TalkativeDirective(e);
    
    expect(directive).toBeTruthy();
  });
});
